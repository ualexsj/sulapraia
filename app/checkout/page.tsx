import type { Metadata } from 'next';
import Link from 'next/link';
import { products, site } from '@/lib/data';
export const metadata: Metadata = { title: 'Checkout rápido atacado', description: 'Finalize seu pedido Sulapraia com cupom, frete calculado e Mercado Pago.' };
export default function Checkout() { const total = products.slice(0,2).reduce((s,p)=>s+p.price*p.wholesale,0); return <section className="luxury-gradient px-5 pb-20 pt-32"><div className="mx-auto grid max-w-6xl gap-8 lg:grid-cols-[1fr_.75fr]"><div className="rounded-[2rem] bg-white p-8"><p className="text-xs font-bold uppercase tracking-[.28em] text-gold">Checkout atacado</p><h1 className="mt-3 font-serif text-5xl">Compra rápida e segura</h1><div className="mt-8 space-y-4">{products.slice(0,2).map(p => <div className="flex items-center justify-between rounded-3xl border border-gold/15 p-4" key={p.slug}><div><b>{p.name}</b><p className="text-sm text-stone-500">{p.wholesale} peças • grade atacado</p></div><span>R$ {(p.price*p.wholesale).toFixed(2).replace('.', ',')}</span></div>)}</div><div className="mt-8 grid gap-4 md:grid-cols-2"><input placeholder="Cupom de desconto" className="rounded-2xl border px-4 py-4" /><input placeholder="CEP para cálculo de frete" className="rounded-2xl border px-4 py-4" /></div><div className="mt-5 rounded-3xl bg-ocean/10 p-5 text-ocean">Frete estimado será confirmado automaticamente antes do pagamento.</div></div><aside className="h-fit rounded-[2rem] bg-[#17100d] p-8 text-white"><h2 className="font-serif text-4xl">Resumo do pedido</h2><div className="mt-6 space-y-3 text-stone-300"><p className="flex justify-between"><span>Subtotal</span><b>R$ {total.toFixed(2).replace('.', ',')}</b></p><p className="flex justify-between"><span>Frete</span><b>A calcular</b></p><p className="flex justify-between"><span>Pagamento</span><b>Pix, cartão ou Mercado Pago</b></p></div><Link href={site.mercadoPagoLink} className="mt-8 block rounded-full bg-[#009ee3] px-6 py-4 text-center font-bold">Pagar com Mercado Pago</Link><Link href="/" className="mt-3 block rounded-full bg-white px-6 py-4 text-center font-bold text-ocean">Confirmação elegante</Link></aside></div></section>; }
