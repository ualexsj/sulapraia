import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { posts, site } from '@/lib/data';
export function generateStaticParams() { return posts.map(p => ({ slug: p.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const post = posts.find(p => p.slug === slug); if (!post) return {}; return { title: post.title, description: post.excerpt, keywords: post.keywords, alternates: { canonical: `/blog/${post.slug}` }, openGraph: { title: post.title, description: post.excerpt, type: 'article' } }; }
export default async function BlogPost({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const post = posts.find(p => p.slug === slug); if (!post) notFound(); const schema = { '@context':'https://schema.org', '@type':'BlogPosting', headline: post.title, description: post.excerpt, datePublished: post.date, author: { '@type':'Organization', name:'Sulapraia' }, publisher: { '@type':'Organization', name:'Sulapraia' }, mainEntityOfPage: `${site.url}/blog/${post.slug}` }; return <><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /><section className="luxury-gradient px-5 pb-20 pt-32"><article className="mx-auto max-w-4xl rounded-[2rem] bg-white p-8 shadow-xl md:p-12"><p className="text-xs font-bold uppercase tracking-[.3em] text-gold">Blog SEO • {new Date(post.date).toLocaleDateString('pt-BR')}</p><h1 className="mt-4 font-serif text-5xl leading-tight md:text-6xl">{post.title}</h1><p className="mt-6 text-xl leading-9 text-stone-600">{post.excerpt}</p><div className="mt-8 flex flex-wrap gap-2">{post.keywords.map(k => <span className="rounded-full bg-ocean/10 px-3 py-1 text-sm text-ocean" key={k}>{k}</span>)}</div><div className="prose prose-stone mt-10 max-w-none"><h2>Por que este tema importa para o atacado?</h2><p>Revendedoras e boutiques precisam comprar produtos com alto giro, estética fotogênica e margem saudável. A Sulapraia organiza coleções com cartela premium, modelagens comerciais e materiais de campanha para facilitar a venda.</p><h2>Estratégia para vender mais</h2><p>Combine peças de entrada com modelos desejo, fotografe em luz natural, destaque benefícios como pedido mínimo e use chamadas claras para WhatsApp. URLs amigáveis, descrições completas e schema ajudam o Google a entender o conteúdo.</p><h2>Checklist comercial</h2><ul><li>Defina grade por tamanho e cor antes da campanha.</li><li>Use vídeos curtos para demonstrar caimento e textura.</li><li>Inclua Pix, cartão e Mercado Pago no fluxo de compra.</li><li>Reforce envio nacional e atendimento rápido.</li></ul></div><Link href="/atacado" className="mt-8 inline-block rounded-full bg-ocean px-7 py-4 font-bold text-white">Comprar moda praia no atacado</Link></article></section></>; }
