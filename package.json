{
  "scripts": {
    "check:node": "node scripts/check-node-version.cjs",
    "check:build": "node scripts/check-production-build.cjs",
    "predev": "npm run check:node",
    "dev": "next dev",
    "prebuild": "npm run check:node",
    "build": "next build",
    "prestart": "npm run check:node && npm run check:build",
    "start": "next start",
    "lint": "next build",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:restart": "pm2 restart sulapraia --update-env",
    "pm2:logs": "pm2 logs sulapraia"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "typescript": "latest",
    "postcss": "latest"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  }
}
